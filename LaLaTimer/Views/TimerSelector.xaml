<UserControl x:Class="LaLaTimer.Views.TimerSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:LaLaTimer.ViewModels"
             xmlns:local="clr-namespace:LaLaTimer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance vm:TimerSelectorViewModel}">
    <Grid>
        <ListBox Name="timerSelectComboBox" 
                 SelectionMode="Single"
                 ItemsSource="{Binding Timers}"
                 SelectedItem="{Binding SelectedTimer.Value, Mode=TwoWay}">
            
        </ListBox>
    </Grid>
</UserControl>
